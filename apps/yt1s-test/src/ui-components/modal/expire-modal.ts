/**
 * Expire Modal - TypeScript (Stub)
 * TODO: Full implementation needed
 */

export function showExpireModal(videoTitle: string): void {
  console.log('showExpireModal() called (stub):', videoTitle);
  alert(`Download link expired for: ${videoTitle}`);
}
